# 📂 项目结构与文件功能说明书

> **致设计师与创作者：**
> 如果把这个自动生图平台看作一家**“米其林AI餐厅”**，那么项目文件夹里的文件就是这家餐厅的不同部门和设施。
> 即使不懂代码，了解它们的作用也能帮你更好地掌控这个工具。

**📅 2024-11 更新：餐厅后厨已完成“现代化改造”！**
为了应对更大量的订单和更复杂的菜品，我们把原本拥挤的单人厨房（`comfyui_api_server.py`）升级成了分工明确的专业后厨团队（`backend/` 目录）。

---

## 1. 🧠 核心控制区 (大脑与前台)

这里是让平台“跑起来”的最关键部分。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`batch_generation_dashboard.html`** | **电子菜单** | **【前端界面】** 这是你直接打交道的地方。你不需要看代码，只需要在这个漂亮的网页上点菜（输入提示词）、选择套餐（工作流），就能看到实时的烹饪进度。 |
| **`backend/`** (文件夹) | **现代化后厨** | **【后端核心】** 这是升级后的核心区域。所有的逻辑、数据处理、与AI的沟通都在这里发生。详情请看下方第 2 节。 |
| **`quick_start.sh`** | **一键开业** | **【启动脚本】** 这是一个脚本，双击它（在终端运行）就能自动帮你把服务器跑起来，省去输命令的麻烦。 |

---

## 2. 👨‍🍳 现代化后厨详解 (`backend/` 目录)

为了稳定性和扩展性，我们把原来的“全能厨师”拆分成了专业的流水线团队。

| 子目录/文件 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`src/main.py`** | **餐厅经理** | **【入口】** 整个后端的启动入口。负责开门迎客，初始化数据库，确保各个部门就位。 |
| **`src/routes/`** | **服务员** | **【接口层】** 负责接待 `dashboard.html` 传来的请求（点单），把客人的需求准确地传达给后厨，不负责具体做菜。 |
| **`src/services/`** | **主厨团队** | **【业务逻辑】** 真正的烹饪发生地。`TaskService` 负责统筹订单进度，`WorkflowService` 负责把客人的要求（提示词）转换成具体的烹饪步骤（ComfyUI JSON）。 |
| **`src/adapters/`** | **采购与外联** | **【适配器】** 负责与外部世界打交道。比如 `comfyui/adapter.py` 专门负责跟 ComfyUI（灶台）说话，`storage/` 负责把做好的菜存到云端或本地仓库。这样做的好处是，换了灶台或仓库，不需要换厨师。 |
| **`src/config/`** | **员工手册** | **【配置】** 规定了餐厅的运行参数，比如灶台的地址在哪里、仓库的钥匙是什么。 |
| **`src/database/`** | **记账员** | **【数据库管理】** 专门负责管理 `tasks.db` 这个账本，确保每一笔订单都记录在案，不会因为停电而丢失。 |

---

## 3. 📜 生产配方 (工作流模版)

这些 JSON 文件是你在 ComfyUI 中连好的“节点图”的数字化存档。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`Qwen-Image 文生图（API）.json`** | **招牌菜谱 A** | **【文生图模版】** 定义了“如何用文字生成图片”。里面记录了用什么大模型（Qwen）、用什么采样器、多少步数。后端会读取这个配方来做菜。 |
| **`Qwen-Edit 图生图 (API).json`** | **招牌菜谱 B** | **【图生图模版】** 定义了“如何修改一张现有的图”。比如给线稿上色，或者重绘图片。 |

---

## 4. 📦 仓库区 (数据与素材)

这里存放着原材料和成品。

| 文件夹/文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`generated_images/`** | **出菜口** | **【成品存放】** (本地模式下) 所有生成好的图片都会自动保存在这里。每一张图都有自己的编号，方便查找。 |
| **`uploaded_images/`** | **进货区** | **【素材存放】** 当你进行“图生图”时，你上传的参考图（如线稿、底图）会临时存放在这里，供 AI 读取。 |
| **`tasks.db`** | **流水账本** | **【数据库文件】** 一个小本子，记录了所有的历史订单：谁在几点几分生成了什么图，成功了还是失败了。即使重启电脑，历史记录也不会丢。 |
| **`logs/`** | **监控录像** | **【日志目录】** 记录了系统运行的每一行“碎碎念”。如果系统报错了（炸厨房了），技术人员会第一时间查这个文件夹里的记录来诊断问题。 |

---

## 5. 📘 说明书 (文档)

给人类阅读的使用指南。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`技术架构文档.md`** | **建筑蓝图** | **【核心文档】** 详细描述了系统的技术设计、分层架构和未来规划，是工程师施工的依据。 |
| **`使用指南.md`** | **操作手册** | 教你具体怎么一步步使用这个工具。 |
| **`产品定位.md`** | **商业计划书** | 我们梳理的关于这个工具的商业价值、用户画像和核心竞争力。 |
| **`故障排除快速参考.md`** | **急救指南** | 遇到常见问题（如连不上网、报错）时的自查手册。 |

---

## 6. 🧹 杂物间与归档

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`archived/`** | **旧仓库** | **【归档区】** 存放旧版本的单文件服务器代码（如 `comfyui_api_server_legacy.py`）和其他不再使用的测试脚本。 |
| **`requirements.txt`** | **采购清单** | 列出了这个项目运行所需要安装的所有 Python 库（依赖包）。 |
| **`__pycache__/`** | **缓存** | 电脑自动生成的加速文件，人类可以直接忽略它。 |

---

**💡 总结：**

现在的系统更加健壮了。
*   **前端 (Dashboard)** 负责“点菜”。
*   **后端 (Backend/Services)** 负责“调度”。
*   **ComfyUI** 负责“干活”。
*   **文件** 负责“存储”。

各司其职，即使一个环节出了问题，也很容易定位和修复。
