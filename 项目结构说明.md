# 📂 项目结构与文件功能说明书
> **致设计师与创作者：**
> 如果把这个自动生图平台看作一家**“米其林AI餐厅”**，那么项目文件夹里的文件就是这家餐厅的不同部门和设施。
> 即使不懂代码，了解它们的作用也能帮你更好地掌控这个工具。

---

## 1. 🧠 核心控制区 (大脑与前台)

这里是让平台“跑起来”的最关键部分。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`comfyui_api_server.py`** | **总厨兼经理** | **【后端核心】** 它是整个系统的大脑。它负责接收你的订单（生图请求），把任务分配给灶台（ComfyUI 服务器），监控进度，最后把做好的菜（图片）端给你。它从不休息，一直在后台运行。 |
| **`batch_generation_dashboard.html`** | **电子菜单** | **【前端界面】** 这是你直接打交道的地方。你不需要看代码，只需要在这个漂亮的网页上点菜（输入提示词）、选择套餐（工作流），就能看到实时的烹饪进度。 |

## 2. 📜 生产配方 (工作流模版)

这些 JSON 文件是你在 ComfyUI 中连好的“节点图”的数字化存档。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`Qwen-Image 文生图（API）.json`** | **招牌菜谱 A** | **【文生图模版】** 定义了“如何用文字生成图片”。里面记录了用什么大模型（Qwen）、用什么采样器、多少步数。后端会读取这个配方来做菜。 |
| **`Qwen-Edit 图生图 (API).json`** | **招牌菜谱 B** | **【图生图模版】** 定义了“如何修改一张现有的图”。比如给线稿上色，或者重绘图片。 |
| **`角色抠图_透明背景...json`** | **特制菜谱 C** | **【功能性模版】** 专门用于去底、抠图的特定工作流。 |

## 3. 📦 仓库区 (数据与素材)

这里存放着原材料和成品。

| 文件夹/文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`generated_images/`** | **出菜口** | **【成品存放】** 所有生成好的图片都会自动保存在这里。每一张图都有自己的编号，方便查找。 |
| **`uploaded_images/`** | **进货区** | **【素材存放】** 当你进行“图生图”时，你上传的参考图（如线稿、底图）会临时存放在这里，供 AI 读取。 |
| **`tasks.db`** | **流水账本** | **【数据库】** 一个小本子，记录了所有的历史订单：谁在几点几分生成了什么图，成功了还是失败了。即使重启电脑，历史记录也不会丢。 |
| **`logs/`** | **监控录像** | **【日志目录】** 记录了系统运行的每一行“碎碎念”。如果系统报错了（炸厨房了），技术人员会第一时间查这个文件夹里的记录来诊断问题。 |

## 4. 📘 说明书 (文档)

给人类阅读的使用指南。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`README.md`** | **餐厅简介** | 项目的主页介绍，通常包含项目是什么、怎么安装。 |
| **`使用指南.md`** | **操作手册** | 教你具体怎么一步步使用这个工具。 |
| **`产品定位.md`** | **商业计划书** | 我们之前梳理的，关于这个工具的商业价值、用户画像和核心竞争力。 |
| **`故障排除快速参考.md`** | **急救指南** | 遇到常见问题（如连不上网、报错）时的自查手册。 |
| **`UI性能优化记录.md`** | **装修日志** | 记录了我们是如何解决“界面闪烁”等体验问题的，属于技术沉淀。 |

## 5. 🧹 杂物间 (系统与归档)

平时不需要关心的部分。

| 文件名 | 角色类比 | 功能解释 |
| :--- | :--- | :--- |
| **`archived/`** | **旧仓库** | 存放之前测试过的代码、旧版本的方案。虽然现在不用，但为了防止未来要查阅，我们不扔掉，而是收纳起来。 |
| **`requirements.txt`** | **采购清单** | 列出了这个项目运行所需要安装的所有 Python 库（依赖包）。 |
| **`quick_start.sh`** | **一键启动器** | 这是一个脚本，双击它（在终端运行）就能自动帮你把服务器跑起来，省去输命令的麻烦。 |
| **`__pycache__/`** | **缓存** | 电脑自动生成的加速文件，人类可以直接忽略它。 |

---
**💡 总结：**
作为一个创作者，你只需要关注 **`dashboard.html`** (界面) 和 **`generated_images/`** (结果)。
当你需要调整画风时，可能需要更新一下 **`.json`** (配方)。
其他的，都是为了支撑这三者稳定运行的“幕后英雄”。

