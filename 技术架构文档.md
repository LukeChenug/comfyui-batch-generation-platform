下面是一份 工程一号位视角撰写的《技术架构文档》，
结构清晰、给新加入的技术开发也能快速上手，
并且是 纯 Markdown，可直接复制进 Notion / GitHub / 文档系统。

⸻

🚀 ComfyUI-Flow 技术架构文档（工程一号位撰写）

版本：v0.1（2025）
面向对象：后端工程师 / 前端工程师 / 架构师 / 新加入成员
作者：工程负责人（Engineering Lead）

⸻

🧭 一、项目简介（What & Why）

ComfyUI-Flow 是一个面向内容创作者（绘本作者、电商运营、摄影师等）的 AI 生产中台。

它的目标是把复杂的 ComfyUI 工作流、模型、参数控制
封装成简单的 Web 交互，帮助用户像“点单”一样完成图像/视频生产。

你可以把它理解为：

“基于 ComfyUI 的 AI 图像工厂”

我们关注的是流水线式生产，而不是单张“抽卡式生成”。

项目包含三个核心能力：
	1.	业务模板 → ComfyUI Graph JSON 的自动编排
	2.	任务调度、队列管理、算力调度
	3.	对不同云算力提供商的可插拔适配层（Compute Adapter）

本项目的核心设计原则：
	•	📦 解耦算力层（Deploy / RunPod / 自建 GPU）
	•	🔌 采用可替换式 Compute Adapter
	•	🏗️ 业务场景 + 模板 + Workflow 的分层结构
	•	🚦 稳定的任务队列系统
	•	🔍 可观测性（日志、任务状态、错误链路）

⸻

🏛️ 二、总体架构（Architecture Overview）

项目采用 三层架构：前端 → 中台 → 算力层。

┌──────────────────────────────┐
│            前端 Web          │
│  - 参数面板 / 模板选择        │
│  - 上传角色图 / 查看任务结果  │
└───────────────┬─────────────┘
                │ REST / WebSocket
┌───────────────▼─────────────────────────┐
│               中台 Backend API           │
│------------------------------------------│
│ 业务层：任务管理 / 模板选择 / 权限        │
│ 编排层：Prompt + Graph JSON 生成          │
│ 调度层：Queue + 任务状态机               │
│ 适配层：Compute Adapter（关键解耦点）     │
└───────────────┬────────────────────────┘
                │
        ┌───────▼──────────┬────────────────────┐
        │                    │                    │
┌───────▼──────┐     ┌──────▼────────┐     ┌──────▼────────┐
│ ComfyUI Deploy│     │ RunPod GPU    │     │ 自建 GPU 实例  │
│（MVP 阶段使用）│     │（未来扩展）   │     │（企业级扩展）  │
└──────────────┘     └──────────────┘     └──────────────┘

设计亮点：
	•	算力层完全可替换（不会锁死供应商）
	•	前端 → 中台 → 适配层 → 计算层 分工明确
	•	后端不会出现“调用写死 Deploy”这种不可维护行为

⸻

📁 三、目录结构（给开发者的落地结构）

comfyui-flow/
├── apps/
│   ├── web/                     # 前端（可选）
│   └── backend/                 # 后端（中台核心）
│       ├── src/
│       │   ├── routes/          # 接口层
│       │   ├── controllers/     # 控制器层
│       │   ├── services/        # 业务层
│       │   ├── adapters/        # ⭐ Compute Adapter 层
│       │   │   ├── compute/
│       │   │   │   ├── deploy.adapter.ts
│       │   │   │   ├── runpod.adapter.ts
│       │   │   │   ├── mock.adapter.ts
│       │   │   │   ├── types.ts
│       │   │   │   └── index.ts
│       │   │   └── queue/       # 任务队列
│       │   ├── workflows/       # 业务模板
│       │   ├── compiler/        # Prompt + Graph 编排引擎
│       │   ├── models/          # ORM 模型
│       │   ├── db/              # 数据库
│       │   ├── config/          # 配置
│       │   ├── middleware/      # 鉴权等
│       │   ├── utils/           # 工具函数
│       │   └── types/           # 全局类型


⸻

🧩 四、架构分层解析（Developer 必看）

1）前端层（Web）

功能：
	•	展示任务状态（队列/生成进度）
	•	提供业务模板选择，如：
	•	绘本模板
	•	电商图模板
	•	写真模板
	•	参数可视化（prompt、参考图、角色上传）

前端 不与算力平台通信，只通过后端 REST API。

⸻

2）中台（Backend API）

后端是本项目的核心价值。

职责如下：

① 任务管理（Task Management）
	•	创建任务（jobId）
	•	状态管理（pending → running → done/failed）
	•	队列控制（Redis）

② 模板管理（Workflow Templates）
包含：
	•	ComfyUI Graph JSON 模板
	•	参数 schema
	•	Prompt 模板

模板按业务

/workflows/picturebook/v1.graph.json
/workflows/portrait/v1.graph.json

开发者只需要修改模板文件，不会动到算力逻辑。

⸻

③ 核心：Compute Adapter（算力适配层）

这是整个项目最重要的模块。
它让整个后端 不依赖任何单一算力平台。

interface ComputeAdapter {
    run(workflowId: string, payload: any): Promise<JobResult>;
    status(jobId: string): Promise<JobStatus>;
    cancel(jobId: string): Promise<boolean>;
}

当前版本包含：

DeployAdapter（推荐用于 MVP 阶段）
RunPodAdapter（未来扩展）
MockAdapter（开发用）

✨ 为什么 Compute Adapter 是核心？
	•	将“业务逻辑”与“算力/平台差异”完全隔离
	•	以后想从 Deploy → RunPod → 自建，只改一处代码
	•	前端 & 业务层完全不用改

⸻

④ 编排引擎（Prompt + Graph Builder）

负责把：

用户输入参数 → Prompt 文本 → ComfyUI Graph JSON

逻辑流程：
	1.	根据 workflowType 加载模板
	2.	根据用户输入填充 prompt slot
	3.	注入到图的某些节点字段中（固定结构）
	4.	输出一份可直接给算力的 ComfyUI graph JSON

例：

buildGraph('picturebook_v1', {
  character: "小火车咔嚓",
  action: "冲进森林",
  style: "儿童绘本"
});


⸻

⑤ 任务队列（Scheduler）

队列基于 Redis：

job:pending
job:running
job:done
job:failed

后端服务会：
	•	从 pending 拉任务
	•	按 computeRouter 分配到不同 GPU 后端
	•	接收算力回调，写回任务结果

⸻

🖥️ 五、算力层（Compute Layer）

MVP 阶段（推荐）

✔ 使用 ComfyUI Deploy（无需运维 GPU）
✔ 成本透明
✔ 部署快
✔ 非常适合 0→1 验证

⸻

未来（当成本或性能成为瓶颈）

加一个：

RunPodAdapter

流程变成：

热门工作流 → RunPod
冷门 / 小规模任务 → Deploy

最终你会形成“算力混合模式”，成本最低，性能最佳。

⸻

🧪 六、数据流（Request Flow）

1. 用户提交任务

POST /jobs
→ job.service.create()
→ queue.push(job)

2. Worker 消费队列

queue.pop()
→ computeRouter.chooseAdapter()
→ adapter.run()

3. 等待算力完成（回调或轮询）

adapter.status()
→ update DB
→ 通知前端（WebSocket / SSE）

4. 用户查看结果

GET /jobs/:id


⸻

📈 七、架构原则

1. 算力平台不应锁死（Compute Adapter）

我们永远能：
	•	换供应商
	•	换 ComfyUI 版本
	•	加模型
	•	加新工作流类型

2. 模板驱动（Workflows as Configuration）

业务逻辑尽量不要硬编码在 service 内。

3. 编排逻辑集中（Compiler）

Prompt 生成 → Graph 注入 → 都放在 /compiler 层。

4. 队列解耦（Scheduler）

避免直接同步调用算力 API。

5. 任务天然可追踪（TraceId）

每个 job 都应带 traceId。

⸻

🛠️ 八、技术栈

后台：
	•	Node.js + TypeScript（可替换为 Python）
	•	Fastify / Express
	•	Redis（队列）
	•	PostgreSQL / MySQL（任务记录）
	•	pnpm workspace（多模块管理）

前端：
	•	Next.js / React
	•	Tailwind
	•	TanStack Query

算力层：
	•	ComfyUI Deploy（MVP）
	•	RunPod / 自建 GPU（未来）

⸻

🧩 九、未来扩展能力（Roadmap）

阶段	能力	简述
1	MVP（Deploy 单算力）	最快上线
2	RunPodAdapter	热门工作流成本下降
3	模型缓存	避免重复加载模型
4	多机自动扩缩容	用量大时自动加算力
5	模板市场	允许用户定制工作流
6	企业级 SLA	自建算力池，成本最优


⸻

📌 十、总结（给开发者）

ComfyUI-Flow 的核心价值在于：
	•	统一的工作流抽象层（Workflows）
	•	稳定的任务调度系统（Scheduler）
	•	可替换的算力适配层（Compute Adapter）

算力供应商只是插件，不是核心。
真正的核心是：

我们的平台能够将任意业务场景 → 编排成稳定可复用的 AI 生产管线。

⸻

⚠️ 十一、工程落地关键风险与对策（补充必读）

在实际工程落地中，除了架构设计，以下细节决定了项目的成败：

1. 🗄️ 图片/文件存储策略 (Object Storage)
   *   **风险**：目前 Demo 阶段图片存储在 API Server 本地，这在生产环境是**不可接受**的（多实例部署时无法访问，且占用服务器磁盘）。
   *   **对策**：生成的图片应立即上传至对象存储（AWS S3 / 阿里云 OSS）。
   *   **实现**：Adapter 获取到 ComfyUI 图片流后，流式上传到 S3，数据库只存 CDN URL。

2. 🔌 长连接稳定性 (WebSocket Resilience)
   *   **风险**：ComfyUI 原生 WebSocket 连接极其脆弱，易因网络波动、闲置超时断开，导致任务状态“假死”。
   *   **对策**：必须在 Backend 与 ComfyUI 之间实现**心跳保活 (Heartbeat)** 和 **自动重连 (Auto-Reconnect)** 机制。
   *   **兜底**：除了 WebSocket 监听，增加每隔 5-10s 的 HTTP 轮询兜底，确保任务状态最终一致。

3. 🛡️ 内容安全与 Prompt 注入
   *   **风险**：直接将用户输入拼接到 Prompt 中可能导致 Prompt Injection（提示词注入），或生成违规内容。
   *   **对策**：
     *   **清洗**：在 Compiler 层增加过滤器，移除特殊字符和敏感词。
     *   **审核**：接入第三方内容审核 API（如阿里云绿网），对生成结果进行异步审核。

4. 🧩 资源依赖一致性
   *   **风险**：本地开发的 ComfyUI 拥有特定的 Checkpoints/LoRA，但云端环境可能缺失，导致报错。
   *   **对策**：建立**环境清单 (Manifest)**。每次部署或切换 Adapter 时，先调用 ComfyUI 的 `/object_info` 接口校验模型文件哈希，确保环境一致。
